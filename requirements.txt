# MRRC Python 运行时依赖
# M20: 从 Dockerfile 的 pip install 提取，供本地开发复现环境（pip install -r requirements.txt）
# 注意：部分依赖（pyaudio/pyserial/hamlib 等）需系统库，参见 Dockerfile 的 apk/apt 部分

tornado
pyaudio
numpy
pyserial
# hamlib Python 绑定（Dockerfile 通过 apk 安装 py3-hamlib；本地可用 pip 安装）
# pyhamlib 或从系统包管理器安装
websocket-client
# 启动器首次运行生成自签 TLS 证书
cryptography
